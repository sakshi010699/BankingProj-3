import { LocationPinCodeCity } from './location-pin-code-city';

describe('LocationPinCodeCity', () => {
  it('should create an instance', () => {
    expect(new LocationPinCodeCity()).toBeTruthy();
  });
});

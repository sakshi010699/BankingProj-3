import { BeneficiaryDetails } from './beneficiary-details';

describe('BeneficiaryDetails', () => {
  it('should create an instance', () => {
    expect(new BeneficiaryDetails()).toBeTruthy();
  });
});

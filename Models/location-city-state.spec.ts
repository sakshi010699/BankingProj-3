import { LocationCityState } from './location-city-state';

describe('LocationCityState', () => {
  it('should create an instance', () => {
    expect(new LocationCityState()).toBeTruthy();
  });
});
